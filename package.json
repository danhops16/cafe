{
  "name": "cafe-website",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync-menu": "node scripts/sync-menu-from-clover.mjs",
    "build": "npm run sync-menu && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
